#include "main.h"

/**
 * _isupper - checks for uppercase character.
 * @c: contains the int to print
 * Return: 0 - otherwise and 1 - if it is uppercase
 */
int _isupper(int c)
{

return (c >= 'A' && c <= 'Z' ? 1 : 0);
}
